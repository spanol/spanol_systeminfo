const Ram = ({ data }) => {
    // const {} = data;
    return (
        <div className="battery">
            <h1>Ram info:</h1>
        {/* <p>{hasBattery ? "true" : "false"}</p>
        <p>{currentCapacity}</p>
        <p>{percent}%</p>
        <p>{isCharging}</p>
        <p>{model}</p> */}
        <h1>Ram</h1>
        </div>
    )
    }
    
    export default Ram;